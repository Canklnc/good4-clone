{
  "name": "good4-v2-firebase",
  "private": true,
  "type": "module",
  "scripts": {
    "test:rules": "firebase emulators:exec --project demo-good4-v2 --only firestore 'node --test --test-concurrency=1 rules.test.mjs'",
    "test:functions": "firebase emulators:exec --project demo-good4-v2 --only firestore 'npm --prefix functions test'",
    "test:web": "npm --prefix web run build",
    "test": "npm run test:rules && npm run test:functions && npm run test:web"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.0",
    "firebase": "^11.0.0",
    "firebase-tools": "^14.0.0"
  }
}
